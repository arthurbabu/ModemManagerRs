[linux-simple]
modem_apn = "datapro"
modem_user = ""
modem_pass = ""

# ---------------------------------------------------------------------------
# http_get_tls example (HTTPS GET with server authentication)
# ---------------------------------------------------------------------------
http_host = "www.google.com"
http_port = 443
http_path = "/"

# SSL context id (0-5) used for the HTTPS socket.
ssl_context_id = 2

# Path to a LOCAL CA certificate (PEM) that signs the server's chain. It is
# uploaded to the modem as cacert.pem and used for server authentication.
# Leave empty to connect WITHOUT server verification (encrypted only).
# For www.google.com use the Google Trust Services root (e.g. GTS Root R1),
# available from https://pki.goog/repository/ .
ca_cert_path = "r1.pem"

# Verify the certificate hostname matches http_host.
ssl_checkhost = false

# Ignore certificate validity dates (set true if the modem clock is unreliable).
ssl_ignore_localtime = true

mqtt_server = "test.mosquitto.org"
mqtt_port = 1883

# alternative server
#mqtt_server = "broker.hivemq.com"
#mqtt_port = 1883
# TLS:
#mqtt_port = 8883

# Empty means "no user"
mqtt_user = ""
mqtt_pass = ""

# SSL/TLS Configuration
# Set to true to enable SSL/TLS for MQTT
mqtt_use_ssl = false

# SSL context ID (0-5, default: 2)
mqtt_ssl_context_id = 2

# Certificate filename (without UFS: prefix) - must be uploaded to modem first
# Example: "cacert.pem"
mqtt_ca_cert_filename = ""

# SSL/TLS version: "tls10", "tls11", "tls12", "all" (default: "tls12")
mqtt_ssl_version = "tls12"

# Cipher suite: "all" for maximum compatibility, or specific hex value like:
#   "0x0035" = TLS_RSA_WITH_AES_256_CBC_SHA
#   "0x002F" = TLS_RSA_WITH_AES_128_CBC_SHA
#   "0x003D" = TLS_RSA_WITH_AES_256_CBC_SHA256
#   "0xC027" = TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA256
#   "0xC028" = TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA384
# (default: "all")
mqtt_ssl_cipher_suite = "all"

# Authentication mode: "none", "server", "mutual" (default: "server")
mqtt_ssl_auth_mode = "server"

# Enable Server Name Indication (default: true)
mqtt_ssl_sni_enable = true

# Enable hostname verification (default: false)
# Note: Set to false to avoid hostname mismatch issues
mqtt_ssl_checkhost_enable = false

# Ignore local time validation (default: true)
# Note: Set to true for easier testing without NTP sync
mqtt_ssl_ignore_localtime = true